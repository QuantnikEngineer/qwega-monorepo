# config/gcp/dev.yaml
# GCP Cloud Run - Development

env:
  ENVIRONMENT: dev
  LOG_LEVEL: DEBUG
  FEATURE_DEBUG_MODE: "true"
  FEATURE_VERBOSE_ERRORS: "true"
  
  API_BASE_URL: https://dev-api.quantnik.example.com
  
  DB_HOST: dev-postgres.quantnik.internal
  DB_PORT: "5432"
  DB_NAME: quantnik_dev
  
  MAX_WORKERS: "2"
