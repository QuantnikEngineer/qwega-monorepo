You are generating a production-ready enterprise GitLab CI pipeline YAML file for QUANTNIK.

OUTPUT CONTRACT
- Return only valid YAML for the file {{ expected_output_path }}.
- Do not add markdown fences, prose, comments, TODO markers, ellipsis, or explanations.
- Treat the supplied stage plan as authoritative for the core CI jobs. Do not remove, rename, or reorder core jobs.
- You may add only the minimum extra approval or rollout jobs required to implement the resolved enterprise controls.
- Do not invent secrets, includes, runner images, or file paths that were not supplied.
- Every generated job must be executable GitLab CI YAML, not pseudo-YAML.

NON-NEGOTIABLE ENTERPRISE RULES
- Build and publish every resolved container image tag when image publishing is enabled.
- Use the resolved rollout regions, not only the raw user-entered region list.
- If approvals are enabled, use blocking manual jobs with when: manual and allow_failure: false before the first rollout job.
- If rollout is enabled, create one isolated rollout job per resolved region after publish.
- Keep job identifiers stable, deterministic, and enterprise-safe.
- Preserve command ordering inside each script block exactly as supplied.
- Do not collapse multiple rollout regions into one shared job.
- Do not omit approval, rollout, or publish behavior when the resolved enterprise controls require it.

GITLAB CI YAML RULES
- Use top-level keys named workflow, stages, and then one job mapping per generated job.
- Preserve the supplied stage order for the {{ stage_count }} core stages: {{ stage_identifiers }}.
- Each generated job must include a stage key and a script section.
- Use job names derived from the provided stage identifiers or resolved rollout regions.
- Prefer protected environments and explicit needs dependencies when approvals or rollout ordering matter.
- Keep the YAML deterministic and production-safe.

RESOLVED ENTERPRISE CONTROL PLAN
- Image publishing enabled: {{ image_enabled }}
- Image repository: {{ image_repository }}
- Image tags: {{ image_tags }}
- Rollout enabled: {{ rollout_enabled }}
- Requested regions: {{ selected_regions }}
- Resolved rollout regions: {{ resolved_rollout_regions }}
- Approvals enabled: {{ approvals_enabled }}
- Approval approvers: {{ approval_approvers }}
- Approval timeout minutes: {{ approval_timeout_minutes }}

REQUEST SUMMARY
- Original user prompt: {{ user_prompt }}
- Repository URL: {{ repository_url }}
- Branch: {{ branch }}
- Deployment target: {{ deployment_target }}
- Environment: {{ target_environment }}
- Selected tools ({{ tool_count }}): {{ selected_tools }}

APPROVAL CONFIGURATION
{{ approval_config_json }}

RESOLVED ROLLOUT REGIONS JSON
{{ resolved_rollout_regions_json }}

ENTERPRISE CONTROLS JSON
{{ enterprise_controls_json }}

RESOLVED STAGE PLAN AND COMMANDS
{{ stage_context_json }}

NORMALIZED INTENT PAYLOAD
{{ normalized_intent_json }}