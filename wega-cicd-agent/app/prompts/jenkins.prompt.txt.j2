You are generating a production-ready Jenkins declarative pipeline Groovy file for WEGA.

Return only the Jenkinsfile Groovy content.
Do not wrap the answer in Markdown fences.
Do not return YAML, JSON, prose, or explanations.

Requirements:
- Output must be valid Groovy for a declarative Jenkins pipeline.
- Use a top-level `pipeline { ... }` block.
- Use the pipeline name {{ pipeline_name }} in a readable way when appropriate.
- The expected output path is {{ expected_output_path }}.
- Repository URL: {{ repository_url }}.
- Branch: {{ branch }}.
- Selected platform: {{ target_platform }}.
- Deployment target: {{ deployment_target }}.
- Environment: {{ target_environment }}.
- Selected tools: {{ selected_tools }}.
- Selected regions: {{ selected_regions }}.
- Rollout enabled: {{ rollout_enabled }}.
- Resolved rollout regions: {{ resolved_rollout_regions }}.
- Image enabled: {{ image_enabled }}.
- Image repository: {{ image_repository }}.
- Image tags: {{ image_tags }}.
- Approvals enabled: {{ approvals_enabled }}.
- Approval approvers: {{ approval_approvers }}.
- Approval timeout minutes: {{ approval_timeout_minutes }}.

Rules:
- Use `agent any` unless a stronger agent choice is explicitly required by the stage commands.
- Use `checkout scm` for repository checkout and do not invent unsupported source plugins.
- Assume SCM webhook or multibranch configuration handles push and PR triggers outside the Jenkinsfile.
- Implement each requested stage as a Jenkins stage using `steps { sh '''...''' }` where shell commands are needed.
- If approvals are enabled, use an `input` step before rollout stages.
- If rollout is enabled, generate separate rollout stages for each resolved rollout region.
- Preserve the intent of the provided stage commands and enterprise controls.
- Do not invent credentials IDs, Jenkins shared libraries, or plugin-specific steps unless they are unavoidable.

Reference data:
- Stage identifiers: {{ stage_identifiers }}
- Stage identifiers JSON:
{{ stage_identifiers_json }}

- Stage context JSON:
{{ stage_context_json }}

- Normalized intent JSON:
{{ normalized_intent_json }}

- Enterprise controls JSON:
{{ enterprise_controls_json }}