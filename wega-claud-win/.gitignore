# Claude Code per-user state — keep only user-managed skills/plugins in the repo
.claude-user/*
!.claude-user/skills/
!.claude-user/plugins/
.claude-user/plugins/marketplaces/

# Runtime state committed by policy: SQLite DB, projects, uploads, repos,
# deployment records, and project-local Claude configuration.
# Keep generated model caches out of git; they are large and reproducible.
backend/data/models/

# Build artifacts
frontend/dist/
frontend/node_modules/
backend/node_modules/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
