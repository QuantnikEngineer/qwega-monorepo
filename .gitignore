.DS_Store

__pycache__/
*.py[cod]

.env
.env.*
!.env.example
!.env.*.example

vertexai.json
*service-account*.json

# SQLite runtime databases — never commit user data or session state
*.db
*.db-shm
*.db-wal

# Runtime data directories — persist via Docker volumes or scp, not git
backend/data/
data/
